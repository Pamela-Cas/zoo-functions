const data = require('../data/zoo_data');

const { hours, species } = data;

/* Sem parâmetros: horários para cada dia e quais animais disponíveis; */
// const semParam = () => {
//   //const hora = data.hours;
//   const animal = data.species.filter((name) => (species.name))
//   return animal;
// };
// console.log(semParam());

/* const animalName = data.species.filter((name) => (name
  .firstName === employeeName || name.lastName === employeeName));
return employee; */
